const team = ['Krish', 'Mew', 'Joe', 'Micheal']

console.log([...team].sort())
console.log(team)

const dog = {
  name: 'Don',
  color: 'black',
}

// Convert object to array key-value pair
console.log(Object.entries(dog))
